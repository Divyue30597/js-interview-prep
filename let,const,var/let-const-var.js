const myname = "Tomchi";

function first() {
  const age = 40;

  if (age >= 30) {
    var millenial = false;
    const decade = 4;
  }

  console.log(millenial);

  function second() {
    const job = "SE";
    console.log(
      `${myname} is ${millenial} about his ${age}, and his job is ${job}`
    );
  }

  second();
}

first();
